import 'package:flutter/material.dart';

class GetInform extends StatefulWidget {
  const GetInform({Key? key}) : super(key: key);

  @override
  State<GetInform> createState() => _GetInformState();
}

class _GetInformState extends State<GetInform> {

  //String InputGrupp;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blueGrey[900],
      appBar: AppBar(title: Text('Крутое название(типо кампус, но не кампус)'),),
      body: Center(child: Column(children: [
        Text('\n\n\nОткуда ты воин?', style: TextStyle(fontSize: 25, color: Colors.white),),
        Text('"мето для ввода группы"',style: TextStyle(fontSize: 25, color: Colors.white),),
        Image(image: AssetImage('assets/9472994.png'), width: 250, height: 400,)],)
      )
    );
  }
}
